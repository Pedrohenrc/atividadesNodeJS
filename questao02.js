let a = 20
let b = 10
let results = []

const somar = (a, b) => a + b
const subtrair = (a, b) => a - b
const dividir = (a, b) => a / b
const multiplicar = (a, b) => a * b
const resto_divisao = (a, b) => a % b

console.log(`soma: ${somar(a, b)}, subtração: ${subtrair(a, b)}, divisao: ${dividir(a, b)}, multiplicar: ${multiplicar(a, b)}, resto_divisao: ${resto_divisao(a, b)} `)

